<h3><%= t("registration.new_registration") %> Step 2 - 确定批次及线路</h3>

<%= simple_form_for @registration, url: update_registration_path(@registration) do |f| %>

  <%= render :partial => "form", :locals => { :f => f } %>

<% end %>
